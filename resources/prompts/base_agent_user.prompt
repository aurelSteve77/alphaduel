Here is the current market state for anonymized symbols: {{ symbols | join(', ') }}.

Asset labels are ASS1, ASS2, … (not real tickers). Trade only those labels.

{{ market_state }}

Write your analysis using ASS* names only, then end with a fenced JSON actions block.
